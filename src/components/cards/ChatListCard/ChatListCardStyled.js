import styled from "styled-components";
import BaseCard from "../BaseCard";

const ChatListCardStyled = styled(BaseCard)`
  display: flex;
  flex-wrap: no-wrap;
  justify-content: space-between;
  padding: 16px;
`;

export default ChatListCardStyled;
