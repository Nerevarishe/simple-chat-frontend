import styled from "styled-components";
import { rem } from "../../../../utils/utils";

const ChatMessagePosition = styled.div`
  display: flex;
  justify-content: flex-end;
  margin-left: ${rem("51px")};
`;

export default ChatMessagePosition;
