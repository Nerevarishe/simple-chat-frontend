import styled from "styled-components";
import { rem } from "../../../utils/utils";

const ChatMessageCardsPosition = styled.div`
  padding: ${rem("16px 9px 96px 9px")};
`;

export default ChatMessageCardsPosition;
