import styled from "styled-components";

const HeaderSideBarItemIcon = styled.span`
  @media (min-width: 1200px) {
    font-size: 60px;
    color: ${(props) => props.theme.colors.whiteColor};
  }
`;

export default HeaderSideBarItemIcon;
