import styled from "styled-components";
import { rem } from "../../../utils/utils";

const FooterButtonWrapper = styled.li`
  width: ${rem("40px")};
  height: ${rem("40px")};
  font-size: ${rem("40px")};
`;

export default FooterButtonWrapper;
